#include "main.h"

/**
 * positive_or_negative - check positive and negative numbers
 *
 * @i:the parameter to be checked
 *
 * Return: always 0
 */
void positive_or_negative(int i)
{
	if (i < 0)
		printf("%d is negative\n", i);
	else
		printf("%d is negative\n", i);
}
